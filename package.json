{
  "name": "gurlz-wellness-ai",
  "version": "1.0.0",
  "description": "GURLZ Wellness AI - Voice-first period companion app",
  "private": true,
  "engines": {
    "node": ">=18 <=22"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "vercel-build": "cd backend && npx prisma generate && cd .. && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "migrate": "cd backend && npm run migrate",
    "seed": "cd backend && npm run seed",
    "test:acceptance": "cd backend && npm run test:acceptance"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@supabase/auth-js": "^2.111.0",
    "@supabase/supabase-js": "2.109.0"
  }
}
